import { PropJsonDirective } from './prop-json.directive';

describe('PropJsonDirective', () => {
  it('should create an instance', () => {
    const directive = new PropJsonDirective();
    expect(directive).toBeTruthy();
  });
});
